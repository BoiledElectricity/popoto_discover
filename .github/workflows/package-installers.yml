name: Package installers

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux:
    name: Linux AppImage and deb
    runs-on: ubuntu-22.04
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Install Linux packaging tools
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            binutils \
            dpkg-dev \
            fakeroot \
            file \
            libfuse2

      - name: Build Linux packages
        run: ./gradlew --no-daemon clean packageHost

      - name: List Linux artifacts
        run: |
          find build/jpackage/artifacts build/jpackage/installer -maxdepth 1 -type f -print -exec ls -lh {} \;

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: popoto-discover-linux
          if-no-files-found: error
          path: |
            build/jpackage/artifacts/*.AppImage
            build/jpackage/installer/*.deb

  macos:
    name: macOS DMG
    runs-on: macos-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Build macOS package
        run: ./gradlew --no-daemon clean packageHost

      - name: List macOS artifacts
        run: |
          find build/jpackage/installer -maxdepth 1 -type f -print -exec ls -lh {} \;

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: popoto-discover-macos
          if-no-files-found: error
          path: build/jpackage/installer/*.dmg

  windows:
    name: Windows MSI
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Install WiX Toolset
        shell: pwsh
        run: |
          choco install wixtoolset --no-progress -y
          choco install imagemagick --no-progress -y
          choco install windowsdriverkit10 --no-progress -y
          $wixBin = "${env:ProgramFiles(x86)}\WiX Toolset v3.14\bin"
          if (Test-Path $wixBin) {
            $wixBin | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }
          Import-Module "$env:ChocolateyInstall\helpers\chocolateyProfile.psm1"
          refreshenv
          $magick = Get-Command magick.exe -ErrorAction SilentlyContinue
          if ($magick) {
            Split-Path $magick.Source | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }

      - name: Build PMM NDIS driver package
        shell: pwsh
        run: |
          .\windows\pmmndis\build.ps1 -Configuration Release -Platform x64
          New-Item -ItemType Directory -Force packaging\windows\pmmndis | Out-Null
          foreach ($name in @("pmmndis630.inf", "pmmndis630.sys", "pmmndis630.cat")) {
            $file = Get-Item "windows\pmmndis\build\package\x64\$name" -ErrorAction SilentlyContinue
            if (-not $file) {
              Write-Error "PMM NDIS driver build did not produce $name"
              exit 1
            }
            Copy-Item $file.FullName "packaging\windows\pmmndis\$name" -Force
          }

      - name: Build Windows package
        shell: pwsh
        run: .\gradlew.bat --no-daemon clean packageHost

      - name: List Windows artifacts
        shell: pwsh
        run: Get-ChildItem build\jpackage\installer | Format-List FullName,Length

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: popoto-discover-windows
          if-no-files-found: error
          path: build/jpackage/installer/*.msi

      - name: Upload PMM NDIS driver package
        uses: actions/upload-artifact@v4
        with:
          name: pmm-ndis-driver-windows
          if-no-files-found: error
          path: packaging/windows/pmmndis/*
